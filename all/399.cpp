#include <iostream>
#include <stack>
#include <queue>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    vector<double> calcEquation(vector<vector<string>>& equations, vector<double>& values, vector<vector<string>>& queries) {
        
    }
};

int main() {
    vector<vector<string>> {{"a", "b"}, {"b", "c"}};
    vector<float> value = {2.0, 3.0};
    vector<vector<string>> queries = {{"a", "c"}, {"b", "a"}, {"a", "e"}};

    Solution solution;
    vector<float> res = solution.calcEquation(equations, values, queries);
    cout << "res: " << endl;
    for (int i: res) {
        cout << i << " ";
    }
    cout << endl;
}